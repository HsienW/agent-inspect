---
"agent-inspect": patch
"@agent-inspect/redact": patch
---

Match credential keys across snake, kebab, and header spellings (`api_key`, `x-api-key`, `access_token`, …) in share/strict export and `@agent-inspect/redact`.
