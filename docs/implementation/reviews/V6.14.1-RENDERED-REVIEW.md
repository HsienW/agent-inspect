# V6.14.1 — Rendered review notes

**Date:** 2026-08-05  
**Method:** Static `apps/website/out` served via `serve` on localhost:3456; HTML/asset HTTP checks (browser MCP unavailable in session).

## Checks

| Route | Status |
|-------|--------|
| `/` | 200 — headline, actively maintained status, pillars present |
| `/docs/`, `/docs/trace-facts/`, `/docs/coding-agent-loop/` | 200 |
| `/llms.txt`, `/ai/product.json`, `/skill.md` | 200 |
| `/robots.txt`, `/sitemap.xml` | 200 (App Router generators) |

## Accessibility / structure

- Semantic headings and FAQ `<details>` retained
- No banned “launch candidate” / “v7 not scheduled” / “matchers are not shipped” strings in homepage HTML
- Mobile layout uses existing responsive Tailwind breakpoints (hero grid, flex wrap CTAs)

## Fixes in this chunk

None required beyond review documentation.
