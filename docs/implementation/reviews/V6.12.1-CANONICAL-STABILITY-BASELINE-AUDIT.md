# V6.12.1 — Canonical Stability Baseline Audit

**Date:** 2026-08-04  
**Baseline:** `agent-inspect@6.12.1` (18 fixed-group packages; schema 1.0)  
**HEAD at audit:** `f070f39` (+ roadmap commit)  
**Authority:** [AGENTINSPECT-CANONICAL-ROADMAP-V6.12.1-TO-V7.md](../AGENTINSPECT-CANONICAL-ROADMAP-V6.12.1-TO-V7.md)

## Publication / ops verification

| Check | Result |
|-------|--------|
| npm `agent-inspect` | **6.12.1** |
| Root `package.json` | **6.12.1** |
| Changeset queue | empty (only `config.json`) |
| Prior ops train | `v6.12-adoption-checkpoint` / `adoption-4` (stale vs new roadmap) |
| AGENTS.md canonical pointer | Still Stability-and-Focus v6.7.3→v7 (must update) |
| New canonical roadmap on disk | Yes |

## Blocker diagnosis (confirmed in source)

| ID | Severity | Symptom | Root cause | Primary files |
|----|----------|---------|------------|---------------|
| **N-1** | High | Completed traces fail incomplete/orphan checks | Semantic consumers use raw persisted rows; v0.1 bridge emits TOOL/LLM `running` start + separate LOGIC completion | `packages/core/src/persisted/from-trace-event.ts`, `packages/core/src/checks/index.ts` (`finishedEvents`, structure rules) |
| **N-3** | High | Required tool “missing” when present | `finishedEvents(..., "TOOL")` excludes bridged terminal LOGIC; nested `metadata.toolName` not read | same + `packages/langchain/src/tool-identity.ts` (write-time only) |
| **N-2** | Medium | Numeric token counts → raw-content UNSAFE | Safety walks `attributes.metadata.tokens.input`; only parents `tokenUsage`/`usage` exempt | `isSafeRawContentMetricPath` in `packages/core/src/checks/index.ts` |

## TraceCheckFacts today

```text
TraceCheckFacts.events = freeze(raw persisted rows for selected run)
```

No logical lifecycle projection module exists. Partial helpers only:

- `computeRunStatus` (ignores running starts for **run** status)
- `tree-bridge` / `pairSteps` (trees/diff, not checks)
- LangChain parent reconciliation (write-time)

## Consumers that must share one facts layer

| Surface | Paths |
|---------|-------|
| Checks / TraceContract | `packages/core/src/checks/index.ts`, `contract.ts`, CLI `check.ts` |
| Suite / gate | `packages/core/src/suite/run.ts`, `packages/core/src/gate/` |
| Eval | `packages/eval`, CLI `eval.ts` (parallel matchers today) |
| Safety / bundle / Evidence | CLI `safety.ts`, `bundle.ts`, evidence views |
| MCP | `packages/mcp-server/src/assess-trace.ts`, `tools.ts` |
| Studio / viewer | project-data / server (read paths) |

## Compatibility decision for 6.12.2

- Keep public experimental `TraceCheckContext.events` as **raw** persisted rows.
- Add additive `logicalEvents` (projection + diagnostics).
- Migrate **built-in** semantic rules to `logicalEvents`.
- Formalize supported `TraceFacts` in **6.13.0** (not this patch).
- No schema 1.0 change; no new packages; no default network.

## Pilot fixtures

No customer-identifying pilot artifacts in-repo. Anonymized pilot-shaped fixtures will be added in 6.12.2-9. Private attestation may be recorded later without fabricating public partners.

## Operational conflict

Old eight-week adoption checkpoint assumed “wait for calendar before development.” Real NestJS/LangGraph pilots exposed flagship-loop correctness defects → product-owner decision supersedes calendar wait. Checkpoint is **superseded, not completed**.

## Next

Activate `v6.12.2-real-pilot-semantic-blockers` and execute [V6.12.2-EXECUTION-PLAN.md](../release-trains/V6.12.2-EXECUTION-PLAN.md).
