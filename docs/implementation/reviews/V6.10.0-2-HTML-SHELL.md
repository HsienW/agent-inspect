# V6.10-2 — Self-contained HTML shell

**Baseline:** `15b639a` (6.10-1)  
**Date:** 2026-08-02  
**Authority:** [V6.10.0-EXECUTION-PLAN.md](../release-trains/V6.10.0-EXECUTION-PLAN.md)

## Delivered

- `buildEvidenceHtmlShell` / `buildEvidenceHtmlShellFromManifest` in `packages/core/src/evidence/html-shell.ts`
- Offline `evidence.html`: inline CSS/JS only, CSP `default-src 'none'`, HTML escaping, bounded embedded JSON
- Keyboard-accessible view nav; print stylesheet; stub panels for later views
- `agent-inspect bundle` writes `evidence.html` and hashes it in `evidence.json`
- Tests: `packages/core/test/evidence/evidence-html-shell.test.ts` + bundle assertion

## Product decisions

| Topic | Decision |
|-------|----------|
| Network | No external CSS/JS/fonts/images |
| Views | Summary filled; other panels stubbed until 6.10-3…5 |
| Embedding | JSON in `<script type="application/json">` with `<`/`>` unicode-escaped |
| XSS | All dynamic text via `escapeHtml`; XSS corpus expands in 6.10-9 |

## Gaps for later chunks

- Tree/timeline/causal views (6.10-3)
- Contract/diff/outcome views (6.10-4)
- Safety/provenance views (6.10-5)

## Classification

Self-contained HTML shell + bundle emit: **CONFIRMED** for 6.10-2.
