# V6.7.3 Stability and Focus — Baseline Audit

**Date:** 2026-08-01
**Published baseline:** `agent-inspect@6.7.3` (npm confirmed for root, `@agent-inspect/langchain`, `@agent-inspect/studio`; Changesets fixed group = 18 packages)
**Source HEAD at audit:** `ddafa51` (`main`)
**Canonical roadmap:** [../AGENTINSPECT-STABILITY-AND-FOCUS-ROADMAP-V6.7.3-TO-V7.md](../AGENTINSPECT-STABILITY-AND-FOCUS-ROADMAP-V6.7.3-TO-V7.md)
**Method:** Classify roadmap P0/P1 against current source/tests. Do not treat roadmap prose as proof a defect still exists.

## Publication / Git state

| Item | Value |
|------|-------|
| Branch | `main` |
| HEAD | `ddafa51` — added updated roadmap |
| Root `package.json` | `6.7.3` |
| npm latest (sampled) | `6.7.3` for `agent-inspect`, `@agent-inspect/langchain`, `@agent-inspect/studio` |
| Dirty worktree at audit start | clean |
| Pending changesets | none |
| GitHub SSH / `gh` from this environment | blocked (DNS / GraphQL Forbidden) — open issues/PRs not inventoried here; re-check before implementing each chunk |

### Commits after published 6.7.3 (unpublished on npm)

| Commit | Summary | Classification impact |
|--------|---------|------------------------|
| `ee49d4c` | stats step-label double-prefix correction (#177) | ALREADY_FIXED_UNPUBLISHED → ship in 6.7.4 / fold into 6.7.4-5 |
| `8e525f1` | completed run status from RUN lifecycle (#176) | ALREADY_FIXED_UNPUBLISHED → ship in 6.7.4 / fold into 6.7.4-2 |
| `71eaa9a` | pre-v7 OSS evidence issue batch (docs) | docs only |
| `ddafa51` | Stability and Focus roadmap | docs only |

Do **not** reimplement `ee49d4c` or `8e525f1`. Include them in the 6.7.4 release notes and golden coverage.

---

## Classification legend

```text
CONFIRMED
PARTIALLY_FIXED
ALREADY_FIXED_UNPUBLISHED
ALREADY_PUBLISHED
NOT_REPRODUCED
SUPERSEDED_BY_CURRENT_ARCHITECTURE
BLOCKED_ON_EXTERNAL_EVIDENCE
REQUIRES_PRODUCT_DECISION
```

---

## Findings

| # | Area | Classification | Source | Tests | Reproducer / notes | Chunk | Effects |
|---|------|----------------|--------|-------|--------------------|-------|---------|
| 1 | Completed-run status from terminal RUN lifecycle / explain vocabulary | **ALREADY_FIXED_UNPUBLISHED** | `packages/core/src/logs/tree-builder.ts` (`computeRunStatus`); `packages/core/src/explain.ts` (`toDisplayStatus`) | `packages/core/test/persisted/tree-bridge.test.ts`; `packages/cli/test/explain.test.ts` | At 6.7.3, TreeBuilder treated any `"running"` step as whole-run `running`. HEAD: RUN terminal status wins; explain maps `ok`→`success`. | Publish in **6.7.4**; golden **6.7.4-2** | Core/CLI; no schema/dep/network |
| 2 | Stats double-prefix `tool:tool:` / `llm:llm:` | **ALREADY_FIXED_UNPUBLISHED** | `packages/core/src/stats.ts` (`formatStepLabel`); names already prefixed in `packages/core/src/step.ts` | `packages/core/test/stats.test.ts` | 6.7.3 prepended type unconditionally. HEAD skips when already prefixed. Timeline may still double-prefix (`packages/core/src/timeline.ts`) — remaining Scope E. | Publish in **6.7.4**; parity **6.7.4-5** | Core render; no schema |
| 3 | Standalone LangGraph/LangChain incomplete runs (`run_completed` missing when all callbacks have `parentRunId`) | **CONFIRMED** | `packages/langchain/src/trace-persistence.ts` (`onStepEnd` completes only if `#rootLcRunId === lcRunId && !lcParentRunId`); `agent-inspect-callback.ts` | Happy-path persistence tests; **no** LangGraph-shaped all-parented completion test | Standalone `persist: true` where every callback has `parentRunId` → root never matches → missing `run_completed` → status `running` | **6.7.4-1** (deepen **6.8**) | `@agent-inspect/langchain`; schema unchanged; no network |
| 4 | Cross-command run-status disagreement | **PARTIALLY_FIXED** | List/view/stats: `trace-metadata.ts`; tree/explain: TreeBuilder + `toDisplayStatus` | Item-1 tests only; no cross-command golden | Completed v0.1 bridge fixed on HEAD unpublished. Incomplete LC runs still make commands agree on wrong `running`. | **6.7.4-2** | Reader/CLI; no schema break |
| 5 | CLI shorthand checks silently filtered | **CONFIRMED** | `packages/cli/src/check.ts` builds rules from `--required-tool` / `--forbidden-tool` but `select` defaults to `["run.status"]`; `checks/index.ts` `selectRules` filters | `packages/cli/test/check.test.ts` lacks `--required-tool` coverage | `agent-inspect check trace.jsonl --required-tool search_docs` → rule constructed then dropped | **6.7.4-3** | CLI options; checks API unchanged if select auto-expanded |
| 6 | Human tool identity vs wrapper class names | **CONFIRMED** | `agent-inspect-callback.ts` `handleToolStart`: `serializedLabel(tool)` for step name; `runName` only in attrs | No DynamicStructuredTool / `metadata.toolName` precedence tests | Tool `runName: "get_navan_rewards"` + class `DynamicStructuredTool` → step named after class | **6.7.4-4** (contract **6.8**) | LangChain display/metadata only |
| 7 | Filtered search ordering (newest-first) | **CONFIRMED** | `packages/core/src/search.ts` sorts ascending (`ta - tb`) then `slice(0, limit)` | No newest-first / limit-keeps-recent test | Multiple matches → oldest first; `--limit` drops newest | **6.7.4-5** | Core search; deterministic behavior change |
| 8 | Bundle refusal exit-code | **ALREADY_PUBLISHED** | `packages/cli/src/bundle.ts` sets `exitCode = 1` on safety fail | `packages/cli/test/bundle.test.ts`; core bundle tests | Unsafe without `--allow-unsafe` → exit 1 already in **6.7.3** | **6.7.4-6** regression lock only | CLI; local safety; no network |
| 9 | Doctor package-resolution contradictions | **CONFIRMED** | `packages/cli/src/doctor.ts` resolves `${name}/package.json`; many packages do not export `package.json` | `packages/cli/test/doctor.test.ts` (workspace smoke) | Packed consumer: entry resolves but `…/package.json` fails → false “not installed” | **6.7.5-0 / 6.7.5-1** | CLI DX only |
| 10 | `better-sqlite3` / Studio/index vs core install | **PARTIALLY_FIXED** + **BLOCKED_ON_EXTERNAL_EVIDENCE** (Node 26) | Studio/index hard-depend on `better-sqlite3@^11.8.0`; eager import in `studio/src/db.ts`, `index-sqlite/src/builder.ts`; root has no sqlite | `packages/core/test/package-boundaries.test.ts` | Core-only install is native-free (**confirmed**). Studio/index can fail hard on native build. Node 26 prebuild failure not reproduced here. | **6.7.5-2 / 6.7.5-3** | Optional packages only; keep SQLite out of root/core |
| 11 | Persisted absolute `traceDir` paths | **CONFIRMED** | `agent-inspect-callback.ts` `#baseAttrs` writes `traceDir` onto step attrs | Persistence tests use absolute `os.tmpdir()` paths | Absolute user path lands in event attributes | **6.7.5-4** | Adapter metadata hygiene; additive field policy |
| 12 | Jest reporter silent no-association | **CONFIRMED** | `packages/jest/src/index.ts` `handleTestCase`: early return when no association | `packages/jest/test/index.test.ts` asserts empty diagnostics | Failed test without association → no artifact, no diagnostic | **6.7.5-5** | `@agent-inspect/jest` only |
| 13 | Safety FPs (UUID/path/email/card) | **REQUIRES_PRODUCT_DECISION** (+ corpus **BLOCKED_ON_EXTERNAL_EVIDENCE**) | Core safety checks; `packages/cli/src/safety.ts`; `packages/redact` | Redact tests exist; no LangGraph metadata FP corpus | Need source-vs-artifact policy + FP corpus before claiming precision | **6.9** | Semantic minor; share policy; no default upload |
| 14 | MCP server bin / SDK / read-only | **PARTIALLY_FIXED** | `packages/mcp-server` read-only tools; **no** `bin` in package.json | `packages/mcp-server/test/*.test.ts` | Library/stdio works; `npx @agent-inspect/mcp-server` not packaged | **6.11** | Optional package; local stdio; read-only |
| 15 | Portable Evidence v2 gaps | **PARTIALLY_FIXED** | Current bundle CLI + core helpers | Bundle refuse/allow-unsafe tests; no Evidence v2 manifest/HTML | Bundle/share-check exists; not Evidence v2 | **6.10** | Local artifact; redaction-first |

---

## Priority for active train (v6.7.4)

| Priority | Items | Action |
|----------|-------|--------|
| Ship as-is | 1, 2 | Include unpublished commits in 6.7.4 notes; do not reimplement |
| Must fix | 3, 5, 6, 7 | Source-confirmed blockers |
| Harden | 4, 8 | Cross-command golden + packed exit-code regression |
| Defer 6.7.5 | 9–12 | Doctor, native driver, paths, Jest DX |
| Defer minors | 13–15 | 6.9 / 6.10 / 6.11 |

## Conservative notes

- Do not mark item 3 fixed until active-lifecycle completion exists; parentless-root completion is necessary but insufficient for LangGraph.
- Item 8 is already correct in published 6.7.3; 6.7.4-6 is a regression lock.
- Item 10: core is clean; do not claim Node 26 install failure without matrix evidence.
- Open GitHub issues/PRs must be re-checked when network allows before duplicating contributor work.

## Next action

Activate operational state for `v6.7.4-real-integration-blockers` and begin chunk **6.7.4-0** (focused reproducers + update this audit if classifications change).
