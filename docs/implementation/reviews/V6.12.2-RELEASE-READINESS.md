# V6.12.2 release readiness

**Train:** `v6.12.2-real-pilot-semantic-blockers`  
**Target:** `6.12.2` (18 fixed-group packages)  
**Date:** 2026-08-04

## Scope delivered

- Additive `logicalEvents` / projection diagnostics on `TraceCheckFacts`
- Pairing of v0.1 run/step start+complete; parent/forest normalization
- Built-in semantic/structure rules + TraceContract use projection
- Canonical tool identity (nested metadata); token metric safety (`tokens`)
- Shared facts inheritance for eval/gate/Evidence/MCP
- Anonymized `fixtures/langgraph/pilot-shaped-bridged-tool.jsonl`
- `scripts/packed-semantic-loop-e2e.mjs` in `pack:smoke`
- Docs: KNOWN-ISSUES, TRACE-CHECKS proposal, CHANGELOG Unreleased

## Compatibility

- Raw public experimental `events` meaning unchanged
- Schema unchanged; no new packages; no new root/core deps; no default network

## Publication path

1. Full release gate (`prepublish:checks` / AGENTS core gate as applicable)
2. Changeset: `.changeset/v6122-logical-lifecycle.md` (patch)
3. Push main → CI → Version Packages PR via `publish.yml`
4. Merge Version Packages (fast-forward / normal merge) → Trusted Publishing
5. Verify `npm view agent-inspect version` → **6.12.2** for all 18 packages

## Non-goals confirmed

No local `npm publish`; no force push; no fabricated pilots; no v7 implementation.
