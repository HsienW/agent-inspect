# 6.12.3 — Cross-surface semantic parity matrix

**Baseline:** 6.12.2 logical projection  
**Date:** 2026-08-04

| Surface | Semantic source | Expected agreement |
|---------|-----------------|--------------------|
| Built-in checks / TraceContract | `logicalEvents` | incomplete/orphan/required-tool |
| Eval / gate / CLI check | `runTraceChecks` | inherits |
| Evidence / bundle check-results | stored check findings | inherits |
| MCP `get_adapter_diagnostics` | `summarizeSemanticParity` | same tool/running counts |
| MCP contract tools | `runTraceChecks` | inherits |
| Safety / verify-safe | raw `events` | intentional (secrets/raw content) |
| list/view status icons | run metadata | run-level only |
| Custom rules | raw `events` unless opted in | documented |

## Framework scaffolding roots

Parents marked `parentMapping: unresolved` / LangGraph sentinel labels do not emit `AI_LOGICAL_PARENT_UNRESOLVED` and remain multi-root forests (not fabricated single roots).

## Private attestation

No fabricated public partners. Pilot evidence remains private/anonymized fixtures only.
