# V6.14.1 — Swarm stability and evidence baseline audit

**Date:** 2026-08-07  
**Baseline:** published `agent-inspect@6.14.1` (18 fixed-group packages)  
**Starting commit:** `aae9d82`  
**Canonical roadmap:** [../AGENTINSPECT-CANONICAL-ROADMAP-V6.14.1-TO-PRE-V7.md](../AGENTINSPECT-CANONICAL-ROADMAP-V6.14.1-TO-PRE-V7.md)  
**Named train:** `agentinspect-swarm-stability-evidence-v6.14.1-to-pre-v7`

## Publication / ops snapshot

| Check | Result |
|-------|--------|
| npm `agent-inspect` | `6.14.1` |
| Root `package.json` | `6.14.1` |
| Branch | `main` @ `aae9d82` |
| Changeset queue | empty (no pending `.changeset/*.md`) |
| Prior mode | `v6.14.x-active-maintenance` / `executionMode: maintenance` |
| Prior roadmap | [../AGENTINSPECT-CANONICAL-ROADMAP-V6.12.1-TO-V7.md](../AGENTINSPECT-CANONICAL-ROADMAP-V6.12.1-TO-V7.md) → historical after this activation |

## Roadmap finding classification vs source

### N-4 — Self-parent relationships (High) — **reproduced in source**

| Claim | Source status |
|-------|---------------|
| Child registered before parent resolve on start | **Confirmed.** `onStepStart` sets `#lcToStepId` and `#registerStepIndexes` before `#resolveParent` ([`trace-persistence.ts`](../../../packages/langchain/src/trace-persistence.ts) ~L371–389). |
| Completion-only synth path same order | **Confirmed.** `#lcToStepId` + `#registerStepIndexes` before `#resolveParent` (~L437–451); similar path ~L529–543. |
| Resolver lacks `excludeStepId` | **Confirmed.** `#resolveParent` / parent reconciliation has no child-self exclusion. |
| Logical projection lacks self-parent diagnostic | **Confirmed.** `LogicalProjectionDiagnostic` union in [`logical-events.ts`](../../../packages/core/src/checks/logical-events.ts) has pair/remap codes only — no `AI_LOGICAL_SELF_PARENT_REMOVED`. |
| Tree builder cycle-unsafe | **Confirmed.** [`tree-builder.ts`](../../../packages/core/src/logs/tree-builder.ts) ~L64–76 links whenever `parentId` exists in map; recursive `assignDepth` with no self/cycle break. TUI [`tree-model.ts`](../../../packages/tui/src/tree-model.ts) ~L101–102 same pattern. |

**Train:** `6.14.2` chunks 0–4, 7–8.

### N-6 — Credential-key false positives (Medium) — **reproduced in source**

| Claim | Source status |
|-------|---------------|
| Substring sensitive-key match | **Confirmed.** `isSensitiveKey` uses `normalized.includes(normalizedKey(sensitive))` ([`checks/index.ts`](../../../packages/core/src/checks/index.ts) ~L1172–1175). |
| `token` matches usage fields | **Confirmed by mechanism.** Keys such as `ls_max_tokens`, `max_tokens`, `token_count` contain normalized `token`. |
| Value-based secrets must remain blocked | **Policy preserved** for 6.14.2 — value detectors stay; key classifier becomes contextual. |
| One shared policy across surfaces | **Gap.** Multiple call sites; parity work is chunk `6.14.2-6`. |

**Train:** `6.14.2` chunks 0, 5–6, 8–9.

### N-5 — Programmatic persisted-trace API friction (Low / Beta DX) — **reproduced in source**

| Claim | Source status |
|-------|---------------|
| No `openTraceFile` / `Directory` / `Text` | **Confirmed.** Grep: absent; only `openTrace` / `readTrace` on structured `TraceInput` ([`readers/index.ts`](../../../packages/core/src/readers/index.ts) ~L2017–2078). |
| `buildTraceFacts` events-only | **Confirmed.** Signature is `readonly PersistedInspectEvent[]` ([`trace-facts.ts`](../../../packages/core/src/checks/trace-facts.ts) ~L83–86). |
| No `evaluateTraceContractRead` | **Confirmed.** Not present; consumers pass events manually (e.g. MCP `buildTraceFacts(read.events)`). |

**Train:** `6.15.0` (not 6.14.2).

## Other roadmap items (deferred trains)

| Item | Classification | Train |
|------|----------------|-------|
| Fidelity classes A–E | Not formalized; [`LANGGRAPH-FIDELITY.md`](../../../docs/LANGGRAPH-FIDELITY.md) still draft-for-v6.8 | 6.15.0 |
| Adapter parent invariants / swarm matrix | Partial via 6.8 contract; deep swarm incomplete due to N-4 | 6.15.0 |
| Evidence-first CI golden paths | Recipes exist; external pilots / retention not fabricated | 6.16.0 |
| 6.14.3 / 6.15.1 | Reserved repair-or-skip only | after each publish |
| v7 | Conditional assessment only | after 6.16.x adoption |

## Non-negotiables (unchanged)

No schema break; no new public packages; no new root/core runtime deps; no default network upload; no weakened redaction; no local `npm publish`; no fabricated pilot evidence.

## Phase 0 outcome

Activate named train `agentinspect-swarm-stability-evidence-v6.14.1-to-pre-v7` with `currentChunk: 6.14.2-0`. No runtime code in this activation commit.
