# V6.8-12 — External LangGraph fidelity validation (HARD STOP)

**Date:** 2026-08-02  
**Authority:** [V6.8.0-EXECUTION-PLAN.md](../V6.8.0-EXECUTION-PLAN.md) · [LANGGRAPH-FIDELITY.md](../../../LANGGRAPH-FIDELITY.md)

## Implementation status (local)

Chunks **6.8-0 … 6.8-11** are on `main` relative to published **6.7.5**:

- Lifecycle state, reuse, parents, synthetic groups, tool identity, persist-by-intent
- `flush` / `finalize` / `close` / `getDiagnostics`
- Real `@langchain/langgraph` no-provider app + parallel/stream/subgraph/error matrix
- NestJS recipe + swarm-shaped handoff recipe

## Gate requirement (not met)

Before publishing **agent-inspect@6.8.0** (fixed group minor):

> Two independent real or genuinely external/high-fidelity LangGraph integrations must pass the published fidelity contract.

| Evidence slot | Status |
|---------------|--------|
| External integration #1 | **MISSING** |
| External integration #2 | **MISSING** |

Local Vitest apps, synthetic fixtures, and in-repo recipes **do not** satisfy this gate.

## Train action

```text
trainStatus: blocked-on-langgraph-validation
```

Do **not**:

- fabricate partner traces or claim external validation
- publish 6.8.0 / open Version Packages for 6.8
- start v6.9+ implementation that assumes 6.8 is published (docs/planning OK; version bumps wait)

## Maintainer next steps

1. Collect two independent LangGraph fidelity evidence packs against `docs/LANGGRAPH-FIDELITY.md`
2. File evidence under `docs/implementation/reviews/` (or agreed path)
3. Resume 6.8-12 → readiness changeset → Version Packages → OIDC publish
4. Then continue Stability train at **v6.9.0**
