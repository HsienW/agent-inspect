# V6.10-4 — Contract / diff / outcome views

**Baseline:** `94bc35b` (6.10-3)  
**Date:** 2026-08-02  
**Authority:** [V6.10.0-EXECUTION-PLAN.md](../release-trains/V6.10.0-EXECUTION-PLAN.md)

## Delivered

- `buildEvidenceContractsViewHtml` — aggregate + bounded finding taxonomy (no evidence payloads)
- `buildEvidenceOutcomesViewHtml` — observed outcomes from redacted persisted events
- `buildEvidenceDiffViewHtml` — empty state, or local run diff when ≥2 runs are bundled
- Bundle fills `contracts` / `outcomes` / `diff` panels in `evidence.html`
- Tests: `packages/core/test/evidence/evidence-views-contract.test.ts`

## Product decisions

| Topic | Decision |
|-------|----------|
| Finding text | Escaped + truncated (200 chars); no `expected`/`actual`/`evidence` blobs |
| Diff | Optional; auto when two or more runs are in the same bundle |
| Outcomes | From redacted artifact events only |

## Gaps for later chunks

- Tools/LLM + safety/provenance views (6.10-5)
- Format modes / verify (6.10-6…7)

## Classification

Contract / diff / outcome views: **CONFIRMED** for 6.10-4.
