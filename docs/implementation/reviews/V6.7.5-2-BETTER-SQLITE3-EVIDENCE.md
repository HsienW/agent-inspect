# V6.7.5-2 — better-sqlite3 update and current Node evidence

**Baseline:** after `aad50b2` (test:all build-before-test)
**Date:** 2026-08-01
**Authority:** [V6.7.5-EXECUTION-PLAN.md](../release-trains/V6.7.5-EXECUTION-PLAN.md) · roadmap §8 Scope B

## Dependency decision

| Candidate | Engines (npm) | Decision |
|-----------|---------------|----------|
| `better-sqlite3@13.0.2` (latest) | `>=22` | **Rejected** for this patch — would drop Node 20 for Studio/index |
| `better-sqlite3@12.11.1` | `20.x \|\| 22.x \|\| 23.x \|\| 24.x \|\| 25.x \|\| 26.x` | **Selected** — keeps Node 20 path; declares Node 26 |

Bumped `@agent-inspect/studio` and `@agent-inspect/index-sqlite` to `better-sqlite3@^12.11.1`. Root/core remain native-free.

## Local install evidence

| OS | Arch | Node | Package | better-sqlite3 | Install | Runtime smoke |
|----|------|------|---------|----------------|---------|---------------|
| darwin | arm64 | v22.22.3 | `@agent-inspect/studio` | 12.11.1 | `prebuild-install` OK | `:memory:` query OK |
| darwin | arm64 | v22.22.3 | `@agent-inspect/index-sqlite` | 12.11.1 | `prebuild-install` OK | `:memory:` query OK |

Focused Vitest: studio + index-sqlite + core package-boundaries — **66 passed**.

## Not claimed (external / matrix gaps)

| Cell | Status |
|------|--------|
| Node 20 LTS install | **Not run here** — leave for CI/community matrix (#157) |
| Node 24 | **Not run here** |
| Node 26 | **Not run here** — declared by upstream engines; do not claim local proof |
| Linux x64 / Windows x64 | **Not run here** |

Do not equate package `engines: >=20` with prebuilt-native support on every future current Node.

## Next

**6.7.5-3** — lazy native-driver boundary so Studio/index fail soft when the driver cannot load.
