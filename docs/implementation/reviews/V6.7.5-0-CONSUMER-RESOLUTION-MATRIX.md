# V6.7.5-0 — Consumer package-resolution matrix

**Baseline:** published `agent-inspect@6.7.4` (`f6d5864` Version Packages merge)
**Date:** 2026-08-01
**Authority:** [V6.7.5-EXECUTION-PLAN.md](../release-trains/V6.7.5-EXECUTION-PLAN.md) · roadmap §8 Scope A

## Method

Focused Vitest matrix: `packages/cli/test/v675-consumer-resolution-matrix.test.ts`

Covers:

- Manifest exports for root + adapters / MCP / redact / eval / Studio / index / Jest
- Entry vs `${name}/package.json` resolution from each package manifest
- Doctor import / version checks at repo root
- True absence of an optional adapter (framework-selected warn)

## Results

| Check | Result | Next chunk |
|-------|--------|------------|
| No package exports `./package.json` | PASS (documented) | Keep; do not add export solely for doctor |
| Entry resolves; `package.json` subpath blocked (`ERR_PACKAGE_PATH_NOT_EXPORTED`) | PASS / CONFIRMED | **6.7.5-1** entry-based resolve |
| Doctor `import-agent-inspect` pass while CJS + version-alignment skipped | PASS (documents contradiction) | **6.7.5-1** |
| Doctor should pass CJS + version-alignment when entry resolves | Fixed in **6.7.5-1** | Done |
| Unknown package → not installed | PASS (non-aliased id) | Keep |

## Required coverage (roadmap)

| Surface | Covered |
|---------|---------|
| root ESM / entry | Yes (resolve `agent-inspect`) |
| root CJS / package.json path | Yes (fails; doctor skips) |
| root version | Yes (doctor skips via same bug) |
| AI SDK / OpenAI Agents / LangChain / MCP server / redact / eval | Manifest + self-manifest resolve |
| optional Studio / index | Manifest matrix |
| monorepo workspace | Repo-root doctor run |
| packed npm consumer | Deferred to 6.7.5-1 / release gate (`pack:smoke` + doctor) |
| package actually absent | Framework `ai-sdk` warn |

## Classification

Doctor package-resolution contradictions remain **CONFIRMED**. Fix in **6.7.5-1**: resolve entry first, walk to nearest `package.json` for version, never classify installed packages as missing solely because `./package.json` is not exported.
