# adoption-1 — Local compatibility evidence

**Date:** 2026-08-02
**Host:** Darwin / arm64
**Node:** v22.22.3
**Baseline commit (before chunk):** `aa461cc`
**Published baseline:** `agent-inspect@6.12.0`

## Commands run (real)

| Command | Result |
|---------|--------|
| `node -p` host probe (`process.version` / platform / arch) | Darwin, arm64, v22.22.3 |
| `pnpm pack:smoke` | PASS (~2.4 min): build + `scripts/package-smoke.mjs` + `scripts/packed-quickstart-e2e.mjs` |
| Workspace ESM import `createInspector` from `packages/core/dist/index.mjs` | PASS (`typeof` function) |
| Workspace CJS `require` `createInspector` from `packages/core/dist/index.cjs` | PASS (`typeof` function) |
| `node packages/cli/dist/index.cjs --help` | PASS (usage / commands listed) |
| `node packages/mcp-server/bin/agent-inspect-mcp-server.cjs --help` | PASS |
| `node packages/mcp-server/bin/agent-inspect-mcp-server.cjs -V` | PASS → `6.12.0` |

`pack:smoke` reported optional package OK for all listed `@agent-inspect/*` packs including `@agent-inspect/mcp-server` and `@agent-inspect/index-sqlite`, plus tarball ESM/CJS/CLI and quickstart e2e on darwin.

## Not run (honest)

| Item | Reason |
|------|--------|
| `node scripts/consumer-compat-matrix.mjs` | Script still writes/replaces `## Consumer compatibility matrix (v6.5.1)`; current ledger uses a different heading — avoided to prevent duplicate/corrupt rows |
| `pnpm compat:smoke` | Overlap with `pack:smoke`; deferred |
| Node 20 / Windows / Linux local | Not available on this host for this chunk |

## Ledger impact

Filled **macOS / Node 22 / ESM** → **pass** in [PRE-V7-ADOPTION-EVIDENCE.md](../PRE-V7-ADOPTION-EVIDENCE.md). Other matrix rows remain `_pending_` / partial / UNTESTED.

## Classification

**CONFIRMED** for adoption-1 (local in-repo evidence only; not external adoption).
