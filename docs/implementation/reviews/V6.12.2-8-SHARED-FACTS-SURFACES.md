# 6.12.2-8 — Shared facts across eval / gate / Evidence / MCP

**Train:** `v6.12.2-real-pilot-semantic-blockers`  
**Date:** 2026-08-04

## Finding

Eval suite (`suite/run.ts`), gate (`gate/run.ts`), CLI check/safety/artifacts, Evidence builders, and MCP `assess-trace` / tools all evaluate semantics through `runTraceChecks` / `evaluateTraceContract` (or CLI wrappers that call the same).

No parallel event walker remains for built-in semantic rules after 6.12.2 projection landing in `TraceCheckFacts.logicalEvents`.

## Inheritance

| Surface | Path to facts | Notes |
|---------|---------------|-------|
| CLI `check` | `runTraceChecks` | Default rules include orphan + raw-content + run.status |
| CLI `safety` / verify-safe | `runTraceChecks` safety rules | Still walk **raw** `events` (by design) |
| Eval suite | `runTraceChecks` | Inherits logical projection |
| Gate | suite/check thresholds | Inherits |
| Evidence / bundle artifacts | stores check results | No new wire format |
| MCP assess | `runTraceChecks` | Inherits |

## Explicit non-change

Custom rules that read `context.events` keep raw-row semantics. Opt into `context.logicalEvents` for lifecycle-aware matching.
