# V6.7.5 — Consumer, native dependency, and DX reliability

**Train:** v6.7.5
**Bump:** Patch
**Baseline:** after `6.7.4` publication
**Authority:** Stability and Focus roadmap §8 · [V6.7.3-STABILITY-AND-FOCUS-BASELINE-AUDIT.md](../reviews/V6.7.3-STABILITY-AND-FOCUS-BASELINE-AUDIT.md)

## Goal

Remove first-minute trust failures: doctor contradictions, optional native SQLite install hardness, absolute `traceDir` in attributes, silent Jest no-association, CLI aliases, blessed NestJS/LangGraph recipe.

## Current assessment

| Item | Classification | Chunk |
|------|----------------|-------|
| Doctor `package.json` export resolve | CONFIRMED | 6.7.5-0 / 6.7.5-1 |
| better-sqlite3 / Node matrix | PARTIALLY_FIXED + BLOCKED_ON_EXTERNAL_EVIDENCE | 6.7.5-2 / 6.7.5-3 |
| Absolute traceDir attrs | CONFIRMED | 6.7.5-4 |
| Jest silent no-association | CONFIRMED | 6.7.5-5 |

## In scope

Consumer resolution matrix; doctor resolver; better-sqlite3 update + evidence; Studio/index lazy native boundary; omit absolute trace paths; Jest diagnostics; backward-compatible CLI aliases; NestJS/LangGraph recipe; release readiness.

## Out of scope

LangGraph fidelity contract (6.8), safety precision (6.9), Evidence v2 (6.10), MCP bin (6.11), new packages, SQLite in root/core, enabling Studio network ingest by default.

## Ordered chunks

```text
6.7.5-0  Consumer package-resolution matrix
6.7.5-1  Doctor resolver and diagnostics
6.7.5-2  better-sqlite3 update and current Node evidence
6.7.5-3  Studio/index lazy native-driver boundary
6.7.5-4  Remove persisted absolute trace paths
6.7.5-5  Jest no-association diagnostics
6.7.5-6  Backward-compatible CLI aliases
6.7.5-7  Blessed NestJS/LangGraph recipe
6.7.5-8  Release readiness
```

## Likely files

`packages/cli/src/doctor.ts`, `packages/studio/src/db.ts`, `packages/index-sqlite/**`, `packages/langchain/src/agent-inspect-callback.ts`, `packages/jest/src/index.ts`, recipes/examples, package.json engines/deps for studio/index-sqlite.

## Impacts

| Dimension | Effect |
|-----------|--------|
| Public API | Additive doctor fields/aliases; optional Jest helper |
| Schema | None |
| Dependencies | Optional package `better-sqlite3` bump only; not root/core |
| Network | None |
| Privacy | Stop persisting absolute local paths by default |

## Gates

Chunk gate = core runtime gate. Release gate = full matrix + native install evidence for documented Node/OS. External: record Node 26 (or current) install results honestly; stop rather than fabricate.

## Proposed commits (abbrev.)

`test: consumer resolution matrix` → `fix(cli): doctor entry-based resolve` → `chore(studio): better-sqlite3 update` → `fix(studio): lazy sqlite driver` → `fix(langchain): omit absolute traceDir` → `fix(jest): no-association diagnostic` → `feat(cli): compat aliases` → `docs: nestjs/langgraph recipe` → `docs: 6.7.5 readiness`

## Affected packages / bump

Fixed group **patch** after 6.7.5-8.
