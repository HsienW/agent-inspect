# V6.9.0 — Safety precision and share policy

**Train:** v6.9.0
**Bump:** Minor
**Baseline:** after `6.8.0` (and any 6.8.x repairs)
**Authority:** Stability and Focus roadmap §10

## Goal

Distinguish source-trace sensitivity from redacted-artifact safety; reduce false positives (UUID/path/email/card/metadata keys); gate bundles on the redacted artifact; keep MCP/CI/Studio parity.

## Current assessment

Category/confidence and source-vs-artifact defaults are recorded in [`docs/SAFETY-POLICY.md`](../../SAFETY-POLICY.md) (6.9-0). Synthetic FP/TP corpus scaffolding lives under `fixtures/safety/` (**CONFIRMED**); detector precision against expected outcomes is **PENDING** through 6.9-3. Existing redact/safety paths remain local and default-safe.

## In scope

Safety policy RFC; finding category/confidence; path-aware raw-content detection; card/UUID/path/email precision; framework metadata sensitivity keys; source-vs-artifact assessment; bundle gating on redacted artifact; explain-findings CLI; override docs; MCP/CI/Studio parity; release readiness.

## Out of scope

Compliance certification claims; default upload; new packages; weakening redaction defaults; Evidence v2 HTML (6.10).

## Ordered chunks

```text
6.9-0   Safety policy RFC and false-positive corpus
6.9-1   Finding category/confidence
6.9-2   Path-aware raw-content detection
6.9-3   Card/UUID/path/email precision
6.9-4   Framework metadata sensitivity keys
6.9-5   Source-versus-artifact assessment
6.9-6   Bundle gating on the redacted artifact
6.9-7   Explain-findings CLI
6.9-8   Custom override documentation
6.9-9   MCP/CI/Studio safety parity
6.9-10  Release readiness
```

## Likely files

`packages/core/src/checks/**`, `packages/redact/**`, `packages/cli/src/safety.ts`, `bundle.ts`, MCP/Studio safety call sites, fixtures for FP/TP corpus.

## Impacts

| Dimension | Effect |
|-----------|--------|
| Public API | Additive finding fields; explain-findings CLI |
| Schema | No break |
| Dependencies | Prefer existing redact/core |
| Network | None |
| Privacy | Stronger precision; no weaker defaults |

## External / manual gates

FP corpus must use synthetic or consented fixtures only. Repair policy: [V6.9.X-SAFETY-REPAIR-POLICY.md](./V6.9.X-SAFETY-REPAIR-POLICY.md).

## Affected packages / bump

Fixed group **minor**.
