# V6.7.4 — Real-integration blocker patch

**Train:** v6.7.4
**Bump:** Patch (fixed group of 18)
**Baseline:** `agent-inspect@6.7.3` + unpublished `8e525f1`, `ee49d4c` on `main`
**Authority:** [../AGENTINSPECT-STABILITY-AND-FOCUS-ROADMAP-V6.7.3-TO-V7.md](../AGENTINSPECT-STABILITY-AND-FOCUS-ROADMAP-V6.7.3-TO-V7.md) · [../reviews/V6.7.3-STABILITY-AND-FOCUS-BASELINE-AUDIT.md](../reviews/V6.7.3-STABILITY-AND-FOCUS-BASELINE-AUDIT.md)

## Goal

Close source-confirmed real-integration blockers so LangGraph-shaped standalone runs complete, CLI shorthand checks fire, tool names are human-readable, renderers/search are consistent, and unpublished correctness fixes ship.

## Product rationale

First serious external integrations exposed incomplete runs, silent check filtering, wrapper-class tool names, and renderer/search inconsistencies. Depth before breadth.

## Current implementation assessment

| Item | Classification | Action |
|------|----------------|--------|
| RUN lifecycle completed status | ALREADY_FIXED_UNPUBLISHED | Ship; golden in 6.7.4-2 |
| Stats double-prefix | ALREADY_FIXED_UNPUBLISHED | Ship; parity in 6.7.4-5 |
| LangGraph incomplete completion | CONFIRMED | 6.7.4-1 |
| Cross-command status golden | PARTIALLY_FIXED | 6.7.4-2 |
| Shorthand check filtering | CONFIRMED | 6.7.4-3 |
| Tool display identity | CONFIRMED | 6.7.4-4 |
| Search newest-first + label parity | CONFIRMED | 6.7.4-5 |
| Bundle refusal exit | ALREADY_PUBLISHED | 6.7.4-6 regression lock |

## In scope

- Active-lifecycle completion for standalone LangChain/LangGraph callbacks
- Cross-command run-status golden
- CLI shorthand → auto-select matching check rules
- Human tool name precedence for display
- Shared step-label formatter; filtered search newest-first
- Bundle exit-code packed regression
- Synthetic LangGraph fixtures (no customer data)
- Packed/compat verification and release readiness

## Out of scope

- Full fidelity contract / flush API (v6.8)
- Doctor / SQLite / Jest DX (v6.7.5)
- Safety precision (v6.9), Evidence v2 (v6.10), MCP bin (v6.11)
- Schema changes, new packages, network behavior, reimplementing `8e525f1`/`ee49d4c`

## Ordered chunks

```text
6.7.4-0  Reproduce every externally reported P0/P1 against current main
6.7.4-1  Adapter active-lifecycle completion
6.7.4-2  Run status cross-command golden
6.7.4-3  CLI shorthand rule auto-selection
6.7.4-4  Tool display identity
6.7.4-5  Shared step-label formatter and search ordering
6.7.4-6  Bundle refusal exit-code regression
6.7.4-7  Synthetic LangGraph fixture corpus
6.7.4-8  Packed package and compatibility verification
6.7.4-9  Release readiness
```

## Likely source files

- `packages/langchain/src/trace-persistence.ts`, `agent-inspect-callback.ts`
- `packages/cli/src/check.ts`, `bundle.ts`
- `packages/core/src/search.ts`, `stats.ts`, `timeline.ts`, `logs/tree-builder.ts`, `explain.ts`
- `fixtures/langgraph/` (new)
- tests under `packages/langchain/test`, `packages/cli/test`, `packages/core/test`

## Exact tests to add

- LangGraph-shaped all-parented tree emits one `run_started` and one terminal `run_completed`
- Semantic parents do not block completion
- `view`/`explain`/`check`/`eval`/`stats` agree on user-facing status for completed and incomplete fixtures
- `--required-tool` / related shorthand flags produce findings without explicit `--select`
- DynamicStructuredTool / `runName` / `metadata.toolName` precedence
- No `tool:tool:` / `llm:llm:` across stats/timeline/search/report
- Filtered search newest-first; `--limit` keeps most recent
- Unsafe bundle packed CLI exit nonzero

## Fixtures / recipes

`fixtures/langgraph/` synthetic corpus (plain-root, semantic-parent, dynamic-tool-name, parallel-children, error-run). No customer metadata.

## Docs

Release readiness; CHANGELOG Unreleased → patch notes; SELF-HOSTING/NETWORK unchanged.

## Impacts

| Dimension | Effect |
|-----------|--------|
| Public API | Additive LangChain completion behavior; CLI select auto-expand; no root export breaks |
| Schema | None (1.0 / 0.1 write unchanged) |
| Dependencies | None |
| Network | None |
| Privacy/security | No absolute-path work here (6.7.5-4); no default upload |
| Migration | Additive; existing parentless-root completion remains |

## Validation

**Focused:** `pnpm exec vitest run <chunk tests>`

**Chunk gate:** `pnpm build && pnpm typecheck && pnpm test && pnpm test:coverage && pnpm size && pnpm test:all && pnpm fixtures:check && pnpm pack:smoke && git diff --check`

**Release gate (6.7.4-9):** full readiness matrix + `pnpm recipes:check` + `pnpm compat:smoke` + `pnpm docs:check` + `pnpm public-truth:check` as applicable.

## External / manual gates

None for publication of 6.7.4 (engineering patch). Do not fabricate LangGraph partner evidence (that gates **6.8**).

## Rollback / repair

Patch-only train; revert individual chunk commits if CI fails. Fidelity deepening deferred to 6.8.x repair policy.

## Proposed commit messages

- `test: reproduce real-integration blockers for 6.7.4`
- `fix(langchain): complete standalone runs via active lifecycle`
- `test: add cross-command run-status golden`
- `fix(cli): auto-select rules for shorthand check flags`
- `fix(langchain): prefer human tool names for display`
- `fix(core): shared step labels and newest-first search`
- `test(cli): lock unsafe bundle refusal exit code`
- `test: add synthetic LangGraph fixture corpus`
- `chore: packed/compat verification for 6.7.4`
- `docs: prepare 6.7.4 release readiness`

## Affected packages / bump

All 18 fixed-group packages via Changeset **patch** after 6.7.4-9 gates pass.
