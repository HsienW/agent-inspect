# V6.15.0 — Release readiness

**Target:** `6.15.0` (fixed-group minor)  
**Baseline:** `6.14.2`  
**Train:** `agentinspect-swarm-stability-evidence-v6.14.1-to-pre-v7`  
**Date:** 2026-08-08

## Scope

LangGraph fidelity classes A–E, relationship conformance, residual scaffolding diagnostics, and additive persisted-trace developer APIs (N-5). No schema change, new package, default-network change, or v7.

## Delivered

- Fidelity classes A–E documented + fixture/matrix coverage
- Relationship conformance fixtures (self-parent, cycle, unresolved, multi-root, synthetic, nested, parallel)
- Ambiguous scaffolding diagnostic (`AI_LANGGRAPH_RELATIONSHIP_AMBIGUOUS`)
- `openTraceFile` / `openTraceDirectory` / `openTraceText` + `invalid_input` guard
- `buildTraceFacts(TraceReadResult)` + `evaluateTraceContractRead`
- Stable `AI_*` programmatic diagnostics with remediation
- ESM/CJS quickstarts + NodeNext/Node16 type tests
- Cross-surface A–E semantic parity matrix

## Gate checklist (roadmap §9.13)

```text
[x] All fidelity classes A–E have deterministic fixtures
[x] Deep swarm check and gate pass (packed E2E retained from 6.14.2)
[x] Programmatic file-to-TraceFacts path works
[x] Programmatic file-to-TraceContract path works
[x] No opaque WeakMap/type errors (bare path → AI_TRACE_INPUT_INVALID)
[x] CLI/API semantic parity on A–E fixtures (core surfaces)
[x] CommonJS and ESM consumers pass
[x] Full release validation commands
[x] Changeset → Version Packages (#203) → Trusted Publishing
[x] npm 6.15.0 verified for all 18 packages
[x] No schema change / no new package / no new default network
```

## Publication result

Published **2026-08-08** via `publish.yml` Trusted Publishing. All 18 fixed-group packages report `6.15.0` on npm.
