# V6.14.2 — Release readiness

**Target:** `6.14.2` (fixed-group patch)  
**Baseline:** `6.14.1`  
**Train:** `agentinspect-swarm-stability-evidence-v6.14.1-to-pre-v7`  
**Date:** 2026-08-07

## Scope

Critical swarm relationship correctness (N-4) and credential-key precision (N-6). No schema change, new package, default-network change, or v7.

## Delivered

- Parent resolution before child index registration (`@agent-inspect/langchain`)
- Capture self-parent invariant (`AI_LANGGRAPH_SELF_PARENT_REJECTED`)
- Logical projection self-parent removal (`AI_LOGICAL_SELF_PARENT_REMOVED`)
- Cycle-safe / visibility-first trees (+ TUI)
- Contextual credential-key classifier (token config ≠ credential)
- Safety parity across check + `@agent-inspect/redact`
- Moderate + deep-swarm fixtures and packed check→gate→bundle→verify E2E
- Known-issues / safety-policy docs

## Gate checklist (roadmap §7.13)

```text
[x] No self-parent persisted by capture order + invariant
[x] Existing self-parent traces normalize without hiding nodes
[x] Deep swarm fixture shows nested LLM and tool
[x] structure.cycle passes on nested-ok swarm
[x] --required-tool get_navan_rewards passes (packed E2E)
[x] ls_max_tokens does not trigger credential/high
[x] Real credentials still fail (access_token)
[x] Evidence v2 bundle verify passes (packed E2E)
[x] Full release validation commands
[x] Changeset → Version Packages (#202) → Trusted Publishing
[x] npm 6.14.2 verified for all 18 packages
```

## Publication result

Published **2026-08-08** via `publish.yml` Trusted Publishing. All 18 fixed-group packages report `6.14.2` on npm.
